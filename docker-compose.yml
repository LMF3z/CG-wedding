services:
  wedding-invitation:
    build: .
    image: invitation-wedding:latest
    container_name: wedding-invitation
    ports:
      - '3001:3001'
    volumes:
      - ./data:/app/data
    restart: unless-stopped
